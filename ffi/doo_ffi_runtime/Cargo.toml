[package]
name = "doo_ffi_runtime"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["staticlib", "rlib"]

[dependencies]
tokio = { version = "1.49.0", features = ["full"] }
tokio-util = { version = "0.7.18", features = ["rt"] }
doo_ffi_core = { path = "../doo_ffi_core" }
num_cpus = "1.17.0"
libc = "0.2.182"
