[package]
name = "doo_ffi_file"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
serde_json = "1.0"
libc = "0.2"
doo_ffi_core = { path = "../doo_ffi_core" }
