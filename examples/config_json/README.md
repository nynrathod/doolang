# Config JSON

**Purpose:** Learn JSON parsing and config handling (no database)

Covers: JSON parsing, config file reading, type-safe access.

## Run

```bash
doo run main.doo
```

---

**For production APIs:** Run `doo init starter`
