# Tasks

**Purpose:** Learn business logic patterns (no database)

Covers: Enums, filtering, store pattern, CLI app.

## Run

```bash
doo run main.doo
```

---

**To build an API version:** See [`examples/tasks_api/`](../tasks_api/)  
**For production APIs:** Run `doo init todo`
