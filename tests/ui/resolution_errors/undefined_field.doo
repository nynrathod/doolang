// ERROR: Undefined
// Tests that accessing an undefined field produces a resolution error

struct User {
    name: Str,
    age: Int
}

fn main() {
    let user = User { name: "Alice", age: 30 };
    print(user.xyz);
}
