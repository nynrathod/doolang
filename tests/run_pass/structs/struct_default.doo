// OUTPUT: Unknown
// OUTPUT: 0
// Tests struct with default values

struct Person {
    name: Str,
    age: Int
}

fn main() {
    let p = Person { name: "Unknown", age: 0 };
    print(p.name);
    print(p.age);
}
