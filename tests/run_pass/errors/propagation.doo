
fn step1() -> Int ! Str { Ok 10; }
fn step2(x: Int) -> Int ! Str { Ok x * 2; }
fn pipeline() -> Int ! Str {
    let a = step1()?;
    let b = step2(a)?;
    Ok b;
}
fn main() {
    let v, e = pipeline();
    if e == nil { print(v); }
}
