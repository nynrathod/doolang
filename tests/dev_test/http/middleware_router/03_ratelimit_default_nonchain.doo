import std::Http::{Server};

fn Ping() -> Str {
    return "ok";
}

fn main() {
    let app = Server.new(":3113");

    app.ratelimit();

    app.get("/ping", Ping);

    app.start();
}
