//
// test_private_import.doo - Test that private (camelCase) types cannot be imported
//

// Try to import private struct (should fail)
import types::config;

// Try to import private enum (should fail)
import types::mode;

fn main() {
    print("This should fail during compilation");
    print("Private types (camelCase) should not be importable");
}
