// ERROR: can only be used inside a loop
// This program should fail to compile because break can only be used inside a loop

fn main() {
    if true {
        break;
    }
    print("done");
}
