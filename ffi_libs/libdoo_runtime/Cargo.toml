[package]
name = "libdoo_runtime"
version = "0.1.0"
edition = "2021"

[lib]
name = "doo_runtime"
crate-type = ["cdylib", "rlib"]

[dependencies]
libc = "0.2"
serde = { version = "1.0.228", features = ["derive"] }
serde_json = "1.0"
regex = "1.10"
