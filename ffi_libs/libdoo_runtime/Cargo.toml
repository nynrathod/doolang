[package]
name = "libdoo_runtime"
version = "0.1.0"
edition = "2021"

[lib]
name = "doo_runtime"
crate-type = ["cdylib"]

[dependencies]
libc = "0.2"
serde_json = "1.0"

[profile.release]
opt-level = 3
lto = true
