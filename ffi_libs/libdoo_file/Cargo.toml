[package]
name = "libdoo_file"
version.workspace = true
edition.workspace = true

[lib]
name = "doo_file"
crate-type = ["cdylib"]

[dependencies]
libc = "0.2"
 doo_runtime = { package = "libdoo_runtime", path = "../libdoo_runtime" }
